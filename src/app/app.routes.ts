import { Routes } from '@angular/router';
import path from 'path';
import { MapComponent } from './map/map.component';

export const routes: Routes = [


{path:'mapc',component:MapComponent}
];
